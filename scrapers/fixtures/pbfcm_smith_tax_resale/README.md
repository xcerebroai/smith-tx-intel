# pbfcm_smith_tax_resale fixtures

`live_capture.pdf` — verbatim PDF capture used for offline `parse_fixture()`.
The adapter's PDF parser (`scrapers/_pdf_text.py`, pure stdlib) is what's
exercised; the PBFCM site itself is stdlib-reachable.
